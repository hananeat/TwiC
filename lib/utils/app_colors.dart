// AppColors is a static class that contains all the colors used across the app,
// to maintain visual consistency and ease of maintenance.
// Every screen should import and use these constants instead of hardcoding color values.

import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFFFFFDE7);
  static const Color yellow = Color(0xFFFFD158);
  static const Color green = Color(0xFF4CAF50);
  static const Color purple = Color(0xFF5D59B5);
  static const Color textDark = Color(0xFF2A2859);
}